h1.light-blue-text.text-accent-2 7 Класс

.section
  .row
    .col.s12
      h4 В помощь ученику для подготовки к проверочным работам:

  .algebra-card
    ul.algebra-list
      - data.algebra.year_2016.documents.each do |document|
        li.algebra-list-section
          .algebra-list-content
            h6
              b= document.title

            - if document.subsections
              ul.algebra-list-subsections
                - document.subsections.each do |sub|
                  li= sub

            - if document.link
              .algebra-list-chip.chip
                b
                  | Документ для скачивания:&nbsp;
                  = link_to(document.link, document.link, target: "_blank")

            - if document.preview
              = image_tag(document.preview, class: "responsive-img")

.section
  .row
    .col.s12
      h4 В данном разделе представлены подготовительные варианты контрольных работ по алгебре:

  .algebra-exams-container
    - data.algebra.year_2016.exams.each do |exam|
      .card
        .algebra-exam-title-wrapper
          .card-title
            = exam.title
        .card-content
          = exam.content
        .card-action
          = link_to("Скачать", "assets/docs/algebra/#{exam.link}", target: "_blank")
